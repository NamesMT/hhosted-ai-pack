node_modules/

# Tracked: the server definitions only. home-hosted's own state (secrets, logs,
# backups) lives under state/, and the servers it supervises write into data/.
state/*
!state/servers.config.json
data/*

# pnpm's virtual store bookkeeping
.pnpm-store/
